# 볼 꼬집기 (cheek-play) — 작업 지침

사진 속 볼을 손가락으로 잡아당기면 늘어나고, 놓으면 출렁이며 돌아오는 웹 시제품. 대상 기기는 갤럭시 스마트폰 브라우저.

## 원칙
- 빌드 없는 정적 사이트다. `index.html` / `style.css` / `app.js` + `privacy.html`. 프레임워크·번들러를 넣지 않는다.
- 사진은 기기 안에서만 처리한다. 업로드, 외부 요청, 외부 CDN 스크립트를 추가하지 않는다.
  얼굴 인식(MediaPipe)도 `vendor/mediapipe/`에 자체 호스팅한다. 번들에 들어 있는 진단 로그 전송(odml.pa.googleapis.com)은 `index.html`의 CSP `connect-src 'self'`로 막는다 — 외부 API를 붙이면 CSP에 도메인을 추가해야 한다. 사용자가 직접 누르는 것만 밖으로 나간다: "의견 보내기" 링크, "이 장면 저장·공유"(공유 시트에서 사용자가 앱을 고름).
- 원본 사진 픽셀은 절대 수정하지 않는다. 변형은 메시 정점의 변위(`dx, dy`)로만 표현하고 항상 0으로 돌아온다.

## 구조 (app.js)
- 좌표계는 이미지 공간: 가로 = 1, 세로 = `A`(세로/가로). 볼 영역 `{cx, cy, r}`도 같은 단위.
- 물리는 CPU 격자(긴 변 128칸, 정점마다 스프링-댐퍼 + 이웃 결합(라플라시안)). **화면은 삼각형 메시가 아니라 픽셀 단위 역방향 워프**: 정점 변위(dx, dy)를 16비트×2로 인코딩한 `dispTex`에 올리고, 프래그먼트 셰이더가 출력 픽셀 p마다 `r + d(r) = p`인 원본 위치 r을 감쇠 고정점 반복(λ=0.7, 8회, 첫·끝은 Catmull-Rom 3차, 중간은 쌍선형)으로 찾아 그 색을 가져온다. **단순히 `p - d(p)`로 한 번만 계산하면 입 쪽 내용이 볼로 끌려오는 엉뚱한 느낌이 난다(실제로 겪음).** 수렴 오차는 최대 당김에서 1.5px 이하. 격자와 무관하게 실루엣이 매끈하고, 세게 당겨도 접히지 않는다. 메시 렌더링으로 되돌리지 말 것(톱니 재발).
- `free[]`가 0인 정점은 고정. 테두리는 항상 고정, "볼 영역만" 모드에서는 영역 마스크 밖도 고정 → 얼굴/배경이 안 늘어난다.
- `step()`은 반드시 **속도 전체 계산 → 위치 전체 갱신** 두 단계. 한 루프로 합치면 진동이 발산한다(실제로 겪음).
- 안정 조건: `(k + 8·kc)·DT² < 4`. `freq` 상한, `kc` 배율, `DT`를 바꿀 때 확인.
- 잡기는 손가락(pointerId)별로 독립(`grabs` Map, 최대 4개). `updateTargets()`가 전부 합쳐 정점별 목표 변위와 `held`를 만들고, `held`가 있는 정점만 단단한 스프링으로 손가락을 따라간다 → 한쪽 볼을 잡은 채 다른 쪽을 놓아도 그쪽은 계속 출렁인다.
- 놀이 중에는 볼 영역 원을 절대 자동으로 띄우지 않는다(사용자 요청). 표시는 편집 모드와 "볼 영역 항상 표시" 옵션뿐.
- 당김 한계 `limit = min(잡는 반지름 × stretch, 1/G)`. G는 잡기 가중치 w의 최대 기울기(격자 유한차분) — 변위 기울기 G·D가 1을 넘으면 사진이 접히므로 자동 제한. 같은 볼을 두 손가락으로 같은 방향으로 당겨도 `updateTargets()`가 held 최댓값으로 나눠 합산 접힘을 막는다.
- 움직임이 멎으면 rAF 루프를 끈다(`wake()`로 재시작). 상시 루프로 바꾸지 말 것.
- `window.__cheek`는 디버그·자동 검증용 훅. `img.decode()`는 백그라운드 탭에서 영원히 안 끝날 수 있어 `decodeImage`는 onload를 쓴다.
- 자동 볼 인식: 사진을 불러오면 `autoPlaceCheeks()`: `nextFrame()`으로 스피너를 먼저 그린 뒤 `detectFull()`(입력 긴 변 1024px). 찾으면 즉시 배치하고, 가장 큰 얼굴이 작으면(폭 < 0.35) `detectTiles()`(3×3 겹침 조각, 조각마다 프레임 양보, 찾은 얼굴이 60% 덮는 조각은 건너뜀)를 **뒤에서** 돌려 추가한다. 0개면 조각 탐색을 기다린다. 40초 타임아웃, 10초 뒤 [직접 맞추기] 버튼. 사진 세대는 `detectToken`(`setImage`에서 증가)으로 구분해 늦게 온 결과를 버린다. 인식 중엔 잡기만 막고 이동·확대는 된다. 모델은 첫 화면 뒤 유휴 시간에 미리 받는다(`preloadLandmarker`, 데이터 절약 모드면 생략). wasm SIMD 없는 기기는 받기 전에 판정(`SIMD_OK`). delegate는 CPU 고정(iOS GPU delegate 불안정). 볼 중심은 랜드마크 `[50,187,205]`/`[280,411,425]` 평균, 반지름은 얼굴 폭(234↔454) × `CHEEK_R_RATIO`.
  사진을 자동으로 확대·자르지 않는다(사용자 요청). 못 찾으면 편집 모드로 넘어간다.
- 보기 조절: `view {z, cx, cy}` → `layout()`이 `imgRect`를 만든다. 첫 손가락이 볼 위면 당기기 모드, 빈 곳이면 보기 모드(한 손가락 이동, 두 손가락 확대, 데스크톱은 휠). 편집 중엔 항상 보기 모드. `#resetView`로 원래 크기.
- 볼 위치 편집: 사진 아래 `#editPanel`(탭 · 방향 버튼 `NUDGE`=1% · 크기 슬라이더). 손가락에 원이 가려서 만든 것이라 없애지 말 것. 선택된 볼만 크기 핸들이 보인다.
- 앞/뒤 판정 `inFrontOf(o, f, reg)`: 폭이 1.5배 이상 크고(어른+아기처럼 애매하면 판정 안 함 — 틀린 보호가 더 눈에 띔), 얼굴 상자가 겹치거나 볼 영향권(1.5r)이 그 얼굴 상자에 닿을 때. 편집으로 원을 다른 얼굴 안(e<1)으로 옮기면 소속을 바꾸고, 그 밖으로 살짝 옮긴 건 소속 유지(`reassignFaces`).
- 여러 명·겹친 얼굴: 정점마다 `owner`(주인 볼)를 정하고, 주인이 다른 이웃과는 라플라시안 결합을 끊는다(한 사람 볼을 당겨도 옆 사람 볼이 안 따라옴). 단 **잡기 가중치는 주인으로 거르지 않는다** — 거르면 두 볼 영역이 겹치는 경계에서 한 칸씩 어긋난 톱니가 생긴다(폰 스크린샷으로 확인한 버그). 자동 인식 시 `faceOvals`(얼굴 타원 + 36점 윤곽 `poly`)를 기록하고, 더 큰(앞) 얼굴의 타원 안쪽은 `protection()`으로 뒷사람 볼 마스크에서 제외한다(정점 단위, 1차 방어).
  2차 방어는 픽셀 단위: 잡힌 볼의 얼굴보다 앞에 있는 얼굴들의 윤곽 폴리곤을 `updateProtectMask()`가 캔버스에 칠해 `maskTex`로 올리고, 셰이더가 그 픽셀의 변위를 0으로 만든다(진짜 누끼). 마스크는 `settle()`까지 유지. 한계: 랜드마크 윤곽이라 머리카락·귀는 보호 안 됨. 마스크 캔버스는 가로·세로 모두 `MASK_PX`배로 찍는다(세로를 /A 하면 3:4 사진에서 위로 찌그러진다 — 전문가 검토로 잡은 버그). 3:4 합성에서 앞얼굴 안쪽 55,440px 중 69px만 변화, 얼굴 위쪽은 정상 이동 확인.
- 브라우저 패널이 숨겨져 있으면 rAF가 멈춰 물리가 안 돈다. 자동 검증은 `__cheek.step()`을 직접 돌리고 `__cheek.render()`로 그릴 것.
- 볼이 작아도 화면 기준 반지름 `MIN_HIT_PX`(28px) 안은 잡힌다.
- 샘플 사진 `samples/baby1~3.jpg`는 코덱스(AI)가 만든 가상의 아기. 볼 좌표는 `SAMPLES`에 미리 계산해 넣어 첫 화면에서 모델을 안 받는다. 사진을 바꾸면 좌표를 다시 계산한다.
- 첫 사용 안내: `cheek.onboarded` 플래그가 없으면 시범 손가락(가짜 포인터 id -1)이 실제 물리로 볼을 당겼다 놓기를 최대 8번 반복 → 사용자가 한 번 당겨 놓으면(이때 플래그 저장) 2단계 카드. 빗나간 탭은 시범을 끊지 않고 "살짝 빗나갔어요" 토스트(사진당 2회). 상단 `?`로 다시 볼 수 있다.
- 의견 보내기: `app.js` 맨 위 `FEEDBACK_URL`에 구글 폼 주소를 넣는다. 비어 있으면 버튼을 숨긴다. `FEEDBACK_PREFILL`에 폼 항목 id를 넣으면 당긴 횟수·얼굴 수·브라우저 종류 같은 숫자만 미리 채운다(사진 없음). `NUDGE_AFTER`(12)번 당긴 뒤 세션당 한 번 부탁 카드, 버튼을 눌러야 `cheek.nudged` 저장.
- 시트(`#pick`/`#sheet`/`#editPanel`)는 `openSheet/closeSheet`로만 열고 닫는다: 하나만 열리고, 열 때 `history.pushState`, 안드로이드 뒤로가기(`popstate`)로 닫힌다. 편집 모드는 `setEditing(on, viaHistory)`.
- 편집 패널: 탭(이름은 `regionLabel`: 얼굴 N 왼볼/오른볼) · 방향 버튼 · 크기 · [볼 추가]/[이 볼 지우기]/[다시 찾기] · 사진을 톡 누르면 선택한 원이 그 자리로. 놀 때 원 흐리게 보이기 옵션은 여기.
- 느낌 조절: 프리셋 3개(말랑/쫀득/탱탱) + 진동 + `<details>` 안에 슬라이더 4개(값은 말로 표시). "볼 영역만 움직이기"는 `?dev=1`에서만 보인다.
- 저장·공유: 당긴 폭이 한계의 절반 이상인 상태에서 놓으면 `captureFrame()`이 FBO(긴 변 1080)에 그려 JPEG blob을 만들고 4초간 [이 장면 저장·공유] 칩. 누르면 첫 회 동의 카드 → `navigator.share({files})`, 안 되면 다운로드. 자동 전송 없음.
- 설정값은 `loadSettings()`가 범위·타입을 검증한다. WebGL 생성·링크 실패는 `fatal()` 카드. 컨텍스트 복구 시 `initGL()`이 마스크·캡처 상태를 초기화한다.
- 진동: 안드로이드는 `navigator.vibrate`, iOS 사파리는 `#iosHaptic`(`<input type=checkbox switch>`)를 `click()`해 시스템 햅틱을 빌린다(iOS 18+ 한정, 강도 조절 불가, `display:none`이면 안 울림). 확실한 해결은 Capacitor 네이티브 햅틱.

## 디자인 (2026-09-23 개편, 사용자 요청: 파스텔·아기자기·눈 편하게)
- 밝은 파스텔 라이트 테마만 있다. 다크 테마로 되돌리지 말 것. 토큰은 `style.css` 맨 위 `:root` (복숭아 우유 배경, 자두빛 글자 `--ink`, 분홍/민트/버터 3색 버튼).
- 서명 요소는 "찹쌀떡 버튼": 알약 모양 + 위쪽 흰 하이라이트 + 아래쪽 색 그림자, 누르면 4px 내려앉음. 하단 3버튼은 색이 다르다(사진=분홍, 볼 위치=민트, 느낌 조절=버터).
- 표시 서체 `Jua`(OFL)는 `fonts/jua.woff2`로 자체 호스팅. **앱에서 쓰는 글자만 남긴 서브셋(71KB)** 이라 화면 문구를 바꾸면 `fonts/README.md` 절차로 다시 만들어야 한다(안 그러면 새 글자만 기본 서체로 나옴). 제목·버튼·카드 문구에만 쓰고 본문·설명은 시스템 서체.
- 캔버스 clearColor는 CSS `--stage`(#FFECEF)와 같아야 한다. 슬라이더 트랙은 직접 그린다(기본은 검게 나옴).
- 좁은 화면(≤430px)에서는 상단 안내문을 숨긴다. 안내 카드와 토스트가 대신한다. 가로 모드(높이 ≤560px)는 버튼이 오른쪽 세로 열로 간다. `color-scheme: only light`로 강제 다크 모드를 막는다.
- 아이콘 `favicon.svg`, `icons/`(180/192/512), 카톡 미리보기 `og.jpg`(1200×630, 절대 URL — 상대 경로 규칙의 유일한 예외). 바꾸면 https://developers.kakao.com/tool/debugger/sharing 에서 캐시 갱신.

## 실행
- 라이브: https://jinavav15000-star.github.io/cheek-play/ (GitHub Pages, `main` 푸시 시 자동 배포). 저장소는 공개.
- 라이브는 `/cheek-play/` 하위 경로다. 모든 경로는 상대 경로(`./`)로 쓴다. 절대 경로(`/vendor/...`)를 쓰면 깨진다.
- `./serve.sh` → 출력되는 주소를 같은 와이파이의 폰에서 연다.
- Claude 미리보기: `.claude/launch.json`의 `cheek-play` (포트 8417).

## 다음 단계 후보
- 전문가 검토 계획 `docs/전문가검토_실행계획_2026-09-24.md` 의 남은 항목: 최근 사진 복원(IndexedDB), 앞/뒤 수동 토글, 회전 재탐색, 촉감 다듬기, PWA(share_target), 인식 워커 이전, 소리.
- 한쪽 볼을 두 손가락으로 집기(pinch): 지금은 두 당김이 단순 합산된다. 두 포인터 사이를 모으는 변위장 추가.
- 머리카락까지 보호하려면 MediaPipe 셀피 다중분할(머리카락 클래스) 추가 검토.
- PWA(오프라인·홈 화면 추가), 정적 호스팅 배포.
